# Back-End

This folder contains the Next.js GraphQL app.

Main route:

- `app/api/graphql/route.ts`: GraphQL endpoint exposed at `/api/graphql`

Useful commands:

- `npm install`
- `npm run codegen`
- `npm run dev`
- `npm run build`
