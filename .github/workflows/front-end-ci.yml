name: Front-End CI

on:
  pull_request:
    types: [labeled, synchronize, reopened]
    paths:
      - "front-end/**"

permissions:
  id-token: write
  contents: read
  pull-requests: write

defaults:
  run:
    working-directory: ./front-end

jobs:
  guard:
    name: Check code-ready label
    runs-on: ubuntu-latest
    outputs:
      ready: ${{ contains(github.event.pull_request.labels.*.name, 'code-ready') }}
    steps:
      - run: echo "Label check complete"
        working-directory: .

  install:
    name: Install
    needs: guard
    if: needs.guard.outputs.ready == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - name: Cache node_modules
        id: cache
        uses: actions/cache@v4
        with:
          path: front-end/node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('front-end/bun.lockb') }}
      - if: steps.cache.outputs.cache-hit != 'true'
        run: bun install

  lint:
    name: Lint & Type Check
    needs: install
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - uses: actions/cache@v4
        with:
          path: front-end/node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('front-end/bun.lockb') }}
      - run: bun run codegen
      - run: bun run lint

  build:
    name: Build
    needs: install
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - uses: actions/cache@v4
        with:
          path: front-end/node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('front-end/bun.lockb') }}
      - run: bun run codegen
      - run: bun run build

  preview:
    name: Preview
    needs: install
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - uses: actions/cache@v4
        with:
          path: front-end/node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('front-end/bun.lockb') }}
